{
	servers {
		protocols h1 h2
	}
	admin off
}

http://35.175.156.119 {
	encode zstd gzip

	@api path /api/* /metrics /health /ready
	handle @api {
		reverse_proxy api:8000
	}

	handle {
		reverse_proxy frontend:3101
	}
}
