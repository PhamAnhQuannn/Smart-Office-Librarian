# This directory is the Alembic migrations folder.
# The canonical Alembic configuration lives in backend/alembic.ini.
# Run migrations from the backend/ directory:
#   alembic upgrade head
#   alembic revision --autogenerate -m "describe change"
